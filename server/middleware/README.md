// README for backend middleware folder
// This folder will contain middleware functions:
// - errorHandler.js: Global error handling ✓ (already created)
// - auth.js: JWT verification, role checking
// - validation.js: Request validation
// - logger.js: Custom logging middleware
// - uploadHandler.js: File upload handling with multer
